// How long a drop zone keeps its "bounce" highlight after a tile lands before it resets.
export const BOUNCE_RESET_MS = 420

// Most pool chips the build view draws; any remaining count beyond this shows as a "+N" badge.
export const MAX_POOL_CHIPS = 12
