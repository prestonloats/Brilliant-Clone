# Project State Report — Balance (Brilliant-Clone Algebra)

_Last generated: **2026-06-24 14:00 UTC (Wednesday)** · Reviewed commit: `85f8986` (tip of the
working branch `cursor/project-efficiency-security-tests-c379`, base `main`) · Generated by an
automated, full-codebase review **that also made code changes** (efficiency, security review, tests)._

This report summarizes the current state of the project: what is done, what remains, known and
potential issues, and prioritized recommendations. It is derived from a fresh full read of the
source, tests, configuration, and the planning documents (`Brilliant Practice PRD.md`,
`Brilliant Backend BrainLift.md`, `BACKEND_ADAPTERS.md`, `PHASE1_QA_CHECKLIST.md`, `README.md`,
`SECURITY.md`), plus a clean `npm ci` and a run of lint, tests, and the production build.

> **What changed this pass (code changes, not a report-only refresh).** Unlike the prior six
> report-only refreshes (the codebase had been stable at `9f8ed3c`), this pass made three concrete
> improvements across the requested areas — **efficiency, security, and tests** — each committed and
> pushed separately:
>
> 1. **Efficiency — fixed the first-load bundle regression (was §6.1).** `vite.config.ts`
>    `manualChunks` now gives the **Firebase SDK its own chunk** and **KaTeX its own chunk** instead of
>    sweeping every dependency into one eagerly-preloaded `vendor` chunk. The Firebase Auth/Firestore
>    SDK is only reached through the dynamic `import()` in `App.tsx`, so it is now a **lazy chunk that is
>    no longer in `dist/index.html`'s modulepreload list** — default local-mode visitors never download
>    it. Result: the eager `vendor` chunk dropped from **1,015.51 kB (303.95 kB gzip) → 189.64 kB
>    (59.65 kB gzip)**, and the SDK (`firebase` chunk **566.50 kB / 166.93 kB gzip**) plus KaTeX
>    (`katex` chunk **257.50 kB / 77.25 kB gzip**) split out. Default first-load JS fell from
>    **~346 kB gzip → ~179 kB gzip** (`index` 42.30 + `vendor` 59.65 + `katex` 77.25 + runtime 0.08).
> 2. **Security — reviewed and verified strong (no fix required).** Clean `npm audit` (**0
>    vulnerabilities**, prod-only too), no unsafe patterns (no `eval`/`new Function`/`innerHTML`/
>    `dangerouslySetInnerHTML`; KaTeX renders via `katex.render` into a ref), no committed secrets (all
>    Firebase config is env-driven; `.env*` is gitignored), hardened `firestore.rules` (per-user
>    ownership, email-verified write gate, path/id matching, append-only attempts, default-deny),
>    document-id injection guard (`assertSafeDocumentId`), comprehensive hosting security headers
>    (CSP/HSTS/nosniff/Referrer-Policy/X-Frame-Options DENY/Permissions-Policy), and CI with
>    `npm audit` + CodeQL `security-extended`.
> 3. **Tests — made more robust (was §6.4 + §6.6/R11).** Added **`tests/equation-latex.test.ts`** (14
>    tests pinning `equationToLatex`/`equationToAriaLabel` behavior, the previously-untested pure
>    converter), and moved MCQ checking into the engine as **`checkMcqStep`** (with 4 new engine tests),
>    removing the escalation logic that was duplicated inline in `App.tsx`. The suite grew **161 → 179
>    tests across 14 files**, all green; lint and the production build pass; `npm audit` clean.
>
> The remaining headline open item is **going live** (§7). The lone build warning (">500 kB chunk")
> now refers only to the **intentionally-lazy** `firebase` chunk, not the eager path.

---

## 1. Executive Summary

**Balance** is a "learn-by-doing" algebra web app modeled on Brilliant.org. It teaches a single
coherent path — **Algebra Foundations** — through interactive, typed lesson steps with instant,
authored feedback. The flagship interaction is a manipulable **balance scale**, joined by an
interactive coordinate grid, slope/intercept sliders, term-tile sorting, and creative
"manipulative" puzzles. Equations are now typeset with **KaTeX** for proper mathematical notation.

**Overall health: strong, and well beyond a minimal MVP.** Build, lint, and the test suite
(**179 tests across 14 files**) are all green, with **0 npm vulnerabilities**. All six lessons are
fully authored and each PRD-signature interaction type is implemented and data-driven. A **real
Firebase backend adapter** (async, email-verified, Firestore with hardened security rules) sits
behind the same app-owned `Backend` contract as the local demo, selectable by env and failing
closed when misconfigured. The repo has **CI, security scanning, and Dependabot**. This pass
**fixed the first-load bundle regression** (Firebase SDK + KaTeX now split into their own chunks;
eager first-load JS ~346 → ~179 kB gzip) and **closed the equation-rendering and MCQ test gaps**.

**The main remaining gap is going live.** The Firebase adapter exists but the app is **not
deployed** (deploy, project linking, App Check, and CSP validation are explicitly left as operator
tasks), so the P0 "deployed and public / concurrency" requirement is unmet in practice. Secondary
gaps still open: **password reset** is not implemented; the **mastery model** is a cumulative
correct/attempts ratio (not the EWMA the docs claim) with **coarse per-skill attribution** (§6.5);
and **documentation has drifted** across `README.md`, `BACKEND_ADAPTERS.md`, and `SECURITY.md` (none
of which mention KaTeX equation rendering). _Resolved this pass:_ the first-load bundle regression
(§6.1), the untested equation-rendering converter (§6.4), and the view-side MCQ duplication (§6.6).

| Area | Status |
| --- | --- |
| Build (`npm run build`) | Passing — app chunk 156.7 kB (42.3 kB gzip); **eager `vendor` chunk now 189.6 kB (59.7 kB gzip)** (React only); **lazy `firebase` chunk 566.5 kB (166.9 kB gzip)**; `katex` chunk 257.5 kB (77.3 kB gzip); CSS 39.6 + 28.8 kB (8.5 + 7.9 kB gzip) |
| Lint (`npm run lint`) | Passing — 0 errors |
| Tests (`npm test`) | Passing — 179/179 across 14 files |
| `npm audit` | 0 vulnerabilities (prod-only too) |
| Local learning loop (lessons 1–6) | Complete and playable; all signature interactions present |
| Equation rendering (KaTeX) | `MathText`/`equationToLatex`; accessible (sr-only plain text + aria); **converter now unit-tested** (`tests/equation-latex.test.ts`, 14 tests). `MathText` itself still needs a DOM harness. |
| Firebase backend adapter | **Implemented** (auth + email verification + Firestore), async, fails closed; default is still `local` |
| Deployment (public URL) | **Not done** (left as operator tasks) |
| CI / security automation | **Present** — `.github/workflows/ci.yml` (lint+build+test), `security.yml` (npm audit + CodeQL), `dependabot.yml` |
| Code-splitting of Firebase SDK | **Fixed (this pass)** — Firebase SDK is its own **lazy** chunk (not modulepreloaded), loaded only in firebase mode; KaTeX split into its own chunk; React-only `vendor` is now ~190 kB |
| MCQ checking | **Unified (this pass)** — `engine.checkMcqStep` is the single source of truth (was duplicated inline in `App.tsx`); engine-tested |
| Docs vs. code consistency | Drifted (README "shells"; `BACKEND_ADAPTERS.md` "EWMA"; `SECURITY.md` "adapters not wired in"; none mention KaTeX) |

---

## 2. What the Project Is (Context)

- **Subject:** Algebra only — a deliberate depth-over-breadth bet (PRD "Non-Goals").
- **Persona:** "Maya," a new-to-algebra 8th grader, mobile-first.
- **Thesis:** Every lesson is a short sequence of *typed, interactive steps* rendered from a data
  model, with sub-100 ms client-side answer checking and specific authored feedback for both right
  and common wrong answers — never a bare red ✗.
- **Course path — a branching DAG** (not a straight line): Balancing Equations → One-Step →
  Two-Step, then Two-Step **diverges** into both **Like Terms** and **Coordinate Plane**, which
  **merge** as prerequisites of **Graphing Lines**. `prerequisites` on each lesson encode the graph;
  `lessonOrder` keeps a valid topological order for recommendations.

---

## 3. Architecture & Tech Stack

**Stack:** React 19.2 + TypeScript ~6.0 + Vite 8 (Rolldown), ESLint 10 / typescript-eslint 8,
Node's built-in test runner. Runtime deps: `firebase` v12.15 (dynamically imported by the adapter,
but see §6.1 for how the SDK still lands in the eager chunk) and **`katex` v0.17** (new; equation
typesetting). No CSS framework (hand-written `App.css` ~39.6 kB built) other than the bundled KaTeX
stylesheet. Dev tooling pins **Node 24.16.0** (`.nvmrc`, `.node-version`, CI); this review machine
ran Node 22.14 and all checks still passed.

**Module layout (`src/`):**

| File / dir | Lines | Responsibility |
| --- | --- | --- |
| `App.tsx` | ~4162 | Entire React UI: all screens, step renderers, drag/keyboard interactions, async data flow |
| `engine.ts` | ~1013 | Pure answer-checkers for the assessed step types + progress/score/mastery-read/branch-aware recommendation/graph |
| `backend.ts` | ~557 | App-owned `Backend` contract (async-capable) + `LocalBackend` + provider factory + validators |
| `firebaseBackend.ts` / `firebaseBackendCore.ts` | ~281 / ~75 | `FirebaseBackend` (Auth + Firestore) + path/owner/verify helpers |
| `MathText.tsx` / `equationLatex.ts` | ~46 / ~49 | **New.** KaTeX renderer (ref-based `katex.render`, a11y fallback) + plain-text→LaTeX converter |
| `content/` | per-file | `types.ts` (394), `course.ts`, `skills.ts`, `lessons/*.ts` (one file per lesson), `examples/` |
| `domain.ts` | ~80 | Barrel re-exporting content types/data + runtime types (profile, progress, mastery, attempts) |
| `authValidation.ts` | ~58 | Pure, provider-neutral email/password form validation (shared by UI + tests) |
| `firebaseConfig*.ts` / `firebaseServices.ts` | ~120 | Env-driven Firebase config + SDK init (lazy) |

**Content model (`content/types.ts`):** A `Lesson` is an ordered array of tagged-union `LessonStep`s.
Step types number **ten**: `concept`, `mcq`, `input`, `operation-choice`, `sequence`, `balance`,
plus four hands-on types — **`manipulative`** (equal-groups / collect / build-product puzzles),
**`plot`** (interactive coordinate grid), **`slider`** (slope/intercept → live line), and
**`dragTerms`** (term-tile sorting). The engine now exposes **nine** pure `check*Step` functions
covering all nine *assessed* step types (MCQ's `checkMcqStep` was added this pass — §6.6); `concept`
needs none. Each lesson file is 192–361 lines, fully authored — **none are "shells"**:

- **Balancing Equations** (277 lines), **One-Step** (282), **Two-Step** (361), **Like Terms &
  Variables Both Sides** (321), **Coordinate Plane** (192), **Graphing Lines** (218).

**Equation rendering (new in `9f8ed3c`):** `equationToLatex` converts the app's plain authoring
notation (e.g. `"3x - 5 = 19"`, `"x / 6 = 2"`, `"x - 5 = 9 -> x = 9"`) into LaTeX with conservative,
fail-soft transforms (multi-letter prose → `\text{}`, `->` → `\rightarrow`, `a/b` → `\frac{a}{b}`,
`*` → `\cdot`). `MathText` renders it via `katex.render` into a ref (no `dangerouslySetInnerHTML`),
with `throwOnError:false`, an `aria-hidden` visual layer, and a visually-hidden plain-text label for
screen readers — a thoughtful accessibility design. KaTeX CSS is imported once in `main.tsx`.

**Design strengths:**

- **Clean separation of concerns.** Content (`content/`), pure logic (`engine.ts`), and persistence
  (`backend.ts` / `firebaseBackend.ts`) are decoupled; the engine functions are pure and well tested.
  Lessons are one file each, which supports parallel authoring.
- **Provider-neutral backend behind one contract.** React talks only to `Backend`; both `LocalBackend`
  and `FirebaseBackend` implement it. The contract is **async-capable** (`MaybePromise`), and the UI
  loads learner data into React state and handles loading/error states (no reads during render).
- **Fails closed.** Selecting `firebase` without working config/SDK throws a clear startup error and
  refuses to silently fall back to local. The adapter glue is dynamically imported (`firebaseBackend`
  4 kB, `firebaseServices` 0.26 kB), and as of this pass the **Firebase SDK has its own lazy chunk**
  (`firebase`, 566 kB) that loads only in firebase mode — it no longer rides the eager vendor chunk (§6.1).
- **Security posture.** `firestore.rules` enforce per-user ownership, require the stored `userId`/`id`
  to match the path, gate learning-data writes on `email_verified`, keep attempts append-only, keep
  content read-only, and apply a default-deny catch-all. Firebase Auth adds email verification
  (send/resend/reload) enforced both client-side and in rules. `firebase.json` ships baseline security
  headers (CSP, HSTS, nosniff, Referrer-Policy, X-Frame-Options, Permissions-Policy).
- **Defensive persistence.** Both backends validate/normalize records; the local backend recovers from
  corrupt JSON, scopes access to the active session user, caches parsed reads, and never persists
  passwords. Firebase mastery/attempts writes use Firestore transactions (concurrency-safe; attempts
  reject overwrite).
- **CI & supply-chain hygiene.** Every push to `main`/`local` and every PR runs lint + build + test; a
  separate workflow runs `npm audit` (prod high/critical fails the build) and CodeQL
  (`security-extended`), plus a weekly schedule. Dependabot groups weekly npm and Actions updates.

---

## 4. Verified Health (this review)

All commands were run against a clean install (`npm ci`, **0 vulnerabilities reported**):

- `npm test` → **179 passing, 0 failing**, across 14 files: `engine.test.ts` + `engine.extra.test.ts`,
  `backend.test.ts` + `backend.extra.test.ts`, `hardening.test.ts`, `authValidation.test.ts`,
  **`equation-latex.test.ts` (new)**, `manipulative-step.test.ts`, and one catalog/behavior test per
  lesson (`lesson-*.test.ts`). Tests compile to `dist-tests/` (CommonJS) and run under `node --test`.
- `npm run lint` → **clean (0 errors).**
- `npm run build` → **succeeds**. Chunks: app `index-*.js` **156.73 kB (42.30 kB gzip)**, React-only
  **`vendor-*.js` 189.64 kB (59.65 kB gzip)**, **lazy `firebase-*.js` 566.50 kB (166.93 kB gzip)**,
  `katex-*.js` 257.50 kB (77.25 kB gzip), `firebaseBackend-*.js` 4.08 kB, `firebaseServices-*.js`
  0.26 kB, `rolldown-runtime` 0.08 kB. CSS: app `index-*.css` 39.63 kB (8.53 kB gzip) + **KaTeX
  `vendor-*.css` 28.83 kB (7.92 kB gzip)**, plus the full KaTeX **font set** (**59**
  `.woff2`/`.woff`/`.ttf` files — ~20 per format — browser-fetched on demand). `dist/index.html`
  modulepreloads only `vendor`, `katex`, and the runtime — **not `firebase`** — so the default
  local-mode first load no longer ships the Auth/Firestore SDK. Vite still prints a ">500 kB chunk"
  warning, but it now refers only to the **intentionally-lazy `firebase` chunk**, not the eager path.

> Tests run on compiled JS under `node --test`; there is still **no JSDOM/browser harness**, so
> `App.tsx` (DOM/React, including the drag interactions) and the `MathText` component have no
> automated coverage — only the manual `PHASE1_QA_CHECKLIST.md`. The pure `equationToLatex`/
> `equationToAriaLabel` converter is **now unit-tested** (`tests/equation-latex.test.ts`), and MCQ
> checking now runs through the engine's tested `checkMcqStep`. Pure engine logic and lesson content
> remain very well covered.

---

## 5. PRD Requirements Traceability

✅ done · 🟡 partial · ❌ not done. (Status is "implemented in code"; items needing a live deploy are
noted.)

| Req | Summary | Status | Notes |
| --- | --- | --- | --- |
| R1/R2 | Email+password + display name; persistent login | ✅* | Via `FirebaseBackend` (email/password + display name; persistent Auth session). *Active only when `VITE_BACKEND_PROVIDER=firebase` is configured; local mode stays passwordless. |
| R3/R4 | Logout; profile screen | ✅ | Both present. |
| R5 | Password reset | ❌ | No `sendPasswordResetEmail`; reset flow not implemented. |
| R6/R8 | Structured content model rendered generically | ✅ | Tagged-union `LessonStep`; renderer maps type→component for all 10 types. |
| R7 | One full interactive lesson | ✅ | All six lessons fully authored. |
| R9/R22/R23 | Authored right + wrong feedback, escalating hint→explanation→reveal | ✅ | `engine.ts` `buildWrongResult` (with a `keepHint` mode for per-option choice escalation). |
| R10 | Ordered course with prerequisites | ✅ | Branching DAG via per-lesson `prerequisites` + `isLessonUnlocked`; `buildLessonGraph` derives split/merge stages. |
| R11 | MCQ with per-option feedback | ✅ | Now checked by the engine's pure **`checkMcqStep`** (single source of truth shared with operation-choice); `MultipleChoiceStep` delegates to it, and it has dedicated engine tests. (Was 🟡: previously duplicated inline in `App.tsx`.) |
| R13 | Balance-scale direct manipulation | ✅ | Pointer drag + apply-to-both-sides ops; recent scale/plot UX polish in `9f8ed3c`. |
| R14 | Drag term tiles into like-term bins | ✅ | `dragTerms` type; pure `checkDragTermsStep`. |
| R15 | Interactive coordinate grid (plot points) | ✅ | `plot` type; pure `checkPlotStep` (points + quadrants). |
| R16 | Sliders driving a live graph | ✅ | `slider` type; pure `checkSliderStep` (slope/intercept w/ tolerance). |
| R17 | Tap-to-build / reorder | ✅ | `sequence` type (plus the creative `manipulative` type, now incl. a "build the total" product puzzle). |
| R18 | Numeric/expression input with equivalence | ✅ | `checkInputStep` accepts `3`, `x=3`, `6/2`, etc.; safely rejects arbitrary expressions. |
| R19/R20 | Real-time, animated responsive visual | ✅ | Balance scale, coordinate grid, and live line all respond to input; reduced-motion respected. |
| R24/N2 | Client-side feedback <100 ms | ✅ | Pure functions; no network in the answer loop. |
| R25/N5 | Resume exact step across sessions **and devices** | ✅* | Same-browser resume (local) + cross-device via Firestore. *Cross-device requires firebase mode configured. |
| R26 | Per-skill mastery | 🟡 | Tracked, but the model is a cumulative correct/attempts ratio (**not** EWMA), and each assessed step updates **all** of the lesson's skills identically (see §6.5). |
| R27/R30 | Unlock + recommend next step | ✅ | Branch-aware recommendation + review suggestion. |
| R28 | Surface review on repeated misses | ✅ | Recommendation at mastery `< 0.65` (`MASTERY_READY_THRESHOLD`). |
| R29 | Per-attempt signals (id, correct, attempts, time) | ✅ | `AttemptEvent` with `msToAnswer`; append-only in Firestore. |
| N1/N3/N4 | Fast first interaction; 60 FPS; prefetched steps | 🟡 | **First-load regression fixed (this pass)**: the Firebase SDK is now a lazy chunk and KaTeX is its own chunk, so the default first load ships **~179 kB gzip** (vendor 60 + app 42 + katex 77 + runtime), down from ~346 kB; the Auth/Firestore SDK is fetched only in firebase mode. Content is bundled so steps advance instantly; 60 FPS is still unmeasured (needs a live deploy). |
| N7/N8 | Mobile/touch, large targets, reduced-motion, non-color feedback | ✅ | Pointer drag **plus** keyboard/tap "Place on pan / Return to tray" fallback; reduced-motion reset; text (not color-only) feedback. |
| N9/N13 | Public deploy holding up under concurrency | ❌ | **Not deployed.** Client-side checking + Firestore make this cheap once hosted; deploy/App Check/CSP validation are operator tasks. |
| N11/N12 | Per-user security rules; HTTPS; hashed creds | ✅* | Hardened `firestore.rules` + email-verified write gate + Firebase-managed credentials + hosting HTTPS/headers. *In effect only once deployed. COPPA/age-gating still unresolved (PRD open question). |

---

## 6. Current Issues (concrete, present today)

### 6.1 ✅ RESOLVED (this pass) — Firebase SDK is now code-split out of the first load

_Previously:_ `vite.config.ts` swept **every dependency into one `vendor` chunk** — React, the
Firebase Auth/Firestore SDK, and KaTeX — and because React/KaTeX are static entry dependencies the
whole `vendor` chunk was modulepreloaded eagerly, so every visitor (including default local-mode
users) downloaded the ~1,015 kB / 304 kB-gzip chunk including the unused Firebase SDK on first load.

_Fix applied:_ `manualChunks` now returns a dedicated `firebase` chunk for `@firebase`/`firebase`
modules and a dedicated `katex` chunk, with everything else (React) in `vendor`. Because the Firebase
SDK is only reached through the dynamic `import()` in `App.tsx`, its chunk is **lazy — no longer in
`dist/index.html`'s modulepreload list** — and loads only when firebase mode is selected. Verified
results: eager `vendor` **1,015.51 → 189.64 kB** (303.95 → 59.65 kB gzip); `firebase` chunk
**566.50 kB / 166.93 kB gzip** (lazy); `katex` chunk **257.50 kB / 77.25 kB gzip**. Default
first-load JS **~346 → ~179 kB gzip**, restoring the README's "Firebase stays out of the default
local bundle" claim. (Possible follow-up: the `firebase` chunk is large but lazy; trimming it further
would need finer-grained Firebase imports.)

### 6.2 KaTeX adds a meaningful first-render payload

Beyond the JS, KaTeX brings a **28.8 kB (7.9 kB gzip) stylesheet** and a **large font set** (the
build emits **59** KaTeX font files — ~20 per format across `.woff2`/`.woff`/`.ttf`, ~1,073 kB total
on disk). Browsers fetch only the glyph formats they need, and only when math renders, but the first
math-heavy screen can incur a brief
font fetch / flash of unstyled math. This is an acceptable trade for correct notation, but it should
be measured against N1 once deployed (and `font-display` / preloading the primary `.woff2` faces is
worth considering).

### 6.3 Documentation drift across three files (none mention KaTeX)

- **`README.md`** still describes "the first three authored lessons, with shell lessons reserving
  the rest," lists "Next Priorities: Fill the Lesson 4-6 shells," and its smoke test references a
  "Lesson 4 shell." All six lessons are fully authored — this understates the work. It also does not
  mention the new KaTeX equation rendering.
- **`BACKEND_ADAPTERS.md`** lists `mastery` as a "per-skill EWMA score," but both backends store a
  plain cumulative ratio (§6.5).
- **`SECURITY.md`** says "No hosted backend yet. Firebase and Supabase adapters are not wired
  into the runtime," which contradicts the implemented, env-selectable `FirebaseBackend`.

### 6.4 ✅ RESOLVED (this pass) — equation converter is now unit-tested

_Previously:_ `equationToLatex`/`equationToAriaLabel` (pure string→string transforms with several
regex branches) shipped with **no tests**. _Fix applied:_ added **`tests/equation-latex.test.ts`**
(14 tests) pinning the documented transforms (LaTeX fractions for `a/b`, the `->` step arrow →
`\rightarrow`, prose-word `\text{}` wrapping *without* re-wrapping the LaTeX command names later
transforms introduce, explicit `*` → `\cdot`, single-letter-variable and coordinate-pair
passthrough) plus the fail-soft edges (empty/whitespace/`null`/`undefined`, trimming, idempotency)
and the screen-reader aria-label transform; `src/equationLatex.ts` was added to `tsconfig.test.json`.
**Still open:** the `MathText` React component itself remains uncovered (it needs a JSDOM/RTL harness
— see §6.4-followup in P2 / the no-interaction-layer-tests risk in §8).

### 6.5 Mastery model: cumulative ratio, not EWMA; coarse skill attribution

Both `LocalBackend` and `FirebaseBackend` store `score = Math.round((correct / attempts) * 100) / 100`
(no recency weighting/decay), so early misses count forever and the model can't support the Phase-3
decay/spaced-repetition plan unchanged. Separately, `App.tsx` updates mastery for **every** `skillId`
of the active lesson with the same signal (`activeLesson.skillIds.map(... updateSkillMastery ...)`),
so multi-skill lessons (e.g. Balancing Equations → `equality` + `inverse-operations`) always show
identical per-skill scores.

### 6.6 ✅ RESOLVED (this pass) — MCQ checking moved into the engine

_Previously:_ there was no `checkMcqStep`; correctness and the hint→explanation→reveal escalation
were reimplemented inline in `MultipleChoiceStep`, and a code comment warned the two "mirror" each
other and could drift. _Fix applied:_ added a pure **`engine.checkMcqStep`** (mirrors
`checkOperationChoiceStep` via the shared `buildWrongResult`, handling `McqStep`'s optional
`step.feedback`) and rewired `MultipleChoiceStep` to delegate to it — the same pattern
`OperationChoiceStepView` already used. All choice-style steps now share one escalation path, and
MCQ has dedicated engine tests (correct answer, attempt-by-attempt escalation, per-option
misconceptions, and the no-step-feedback fallback). All nine assessed step types now have a pure
engine checker (`checkMcqStep` joins the set; only the non-assessed `concept` needs none).

### 6.7 Dead `CourseLessonNode.status` field (now also an unreachable UI state)

Each lesson node in `content/course.ts` carries `status: 'available' | 'locked'`, and the type in
`content/types.ts` was widened to include `'coming-soon'` — but the UI **never reads this field**.
Lock/unlock is computed dynamically from progress + prerequisites, and `comingSoon` is derived from
`lesson.steps.length === 0`, which is **always false** now that all six lessons are authored. So both
the `status` field and the "Coming soon" path-status branch (`getPathStatus`) are dead/unreachable
and can drift from reality.

### 6.8 No React error boundary

Async flows set a user-visible `runtimeError` banner (good), but a render-time exception still has no
error boundary to contain it (and the new `MathText` adds a render-time third-party path). Runtime
monitoring (Crashlytics / Performance, per the PRD) is also not wired.

### 6.9 `App.tsx` is a ~4162-line monolith (and growing)

It holds every screen and every step renderer plus the drag engine, and it grew ~170 lines in the
latest **code** commit (`9f8ed3c`). This is hard to navigate, review, and reuse; it is the natural
next refactor and also the code with the least automated coverage.

### 6.10 Toolchain Node-version sensitivity

The repo pins Node 24.16.0, and `scripts/node24-keepalive-fix.cjs` documents a **Node 24.17.0**
regression that breaks `firebase deploy` (it disables keep-alive as a workaround). Anyone deploying
must use the wrapper, and the pinned/CI Node should be kept off the broken patch. (This review ran
fine on Node 22.14, so local dev is tolerant, but CI and deploy are version-sensitive.)

---

## 7. What Needs To Be Done (gaps to "complete")

**To close the remaining P0 PRD gate (going live):**

- **Deploy.** Link the Firebase project (`firebase use --add`), enable Email/Password Auth + Firestore,
  deploy rules + hosting, then verify the security rules and run a basic concurrent-user smoke test
  (N9/N13). `firebase.json` already configures hosting (site `starting-project-e6700`) and a CSP/header
  baseline; validate and tighten the CSP against the live app (note KaTeX fonts are self-hosted, so
  `font-src 'self'` already covers them), and enable App Check.
- **Implement password reset (R5)** via `sendPasswordResetEmail` and a small UI affordance.

**Quality / correctness:**

- ~~**Fix the bundle regression (§6.1)**~~ — **done this pass:** Firebase SDK and KaTeX are now their
  own chunks; the SDK is lazy and out of the default first load.
- ~~**Test the new equation code (§6.4)**~~ — **done this pass** for the pure converter
  (`tests/equation-latex.test.ts`); the `MathText` component still needs a DOM harness.
- ~~**Move MCQ checking into `engine.ts`** as `checkMcqStep`~~ — **done this pass** with unit tests;
  the duplicated view-side escalation is removed.
- **Fix the mastery model:** move to EWMA (matching the docs) and attribute each attempt to the specific
  skill(s) a step exercises rather than all of a lesson's skills (likely needs a per-step `skillId`).
- **Refresh docs:** update `README.md` (drop "shells"; reflect six authored lessons + the four
  interactions + KaTeX + CI), `BACKEND_ADAPTERS.md` (mastery is a cumulative ratio, not EWMA), and
  `SECURITY.md` (the Firebase adapter is implemented and runtime-selectable).
- **Remove or use** the dead `CourseLessonNode.status` field and the unreachable "Coming soon" branch.
- **Add a React error boundary** and wire basic error/performance monitoring.

---

## 8. Potential / Future Issues (risks to watch)

- **Cross-device sync/conflict semantics.** Firestore writes overwrite whole `LessonProgress` /
  `SkillMastery` documents, so two devices editing offline can clobber each other (last-write-wins).
  Decide merge vs. last-write semantics before heavy multi-device use (the BrainLift flags the same
  question about Brilliant itself).
- **CSP not validated against a live deploy.** The README explicitly TODOs verifying the CSP, replacing
  `style-src 'unsafe-inline'`, and HSTS `preload` after a real deploy; an untested CSP can silently
  break Auth/Firestore traffic or provide false confidence. (KaTeX injects inline styles, which
  interacts with any move away from `'unsafe-inline'`.)
- **COPPA / age gating unresolved** (PRD open question) for a teen audience — must be settled before any
  real account collection.
- **Bundle/content growth.** First-load is now light again (SDK lazy, KaTeX split — §6.1), but content
  is bundled; the PRD anticipates fetch-and-cache as the library grows, and the lazy `firebase` chunk
  is still large (566 kB) if/when many users hit firebase mode.
- **Monitoring gap.** No Crashlytics/Sentry or Firebase Performance wiring yet, so production errors and
  N1/N2/N3 latency would be invisible after launch.
- **Mobile drag uses point hit-testing.** The keyboard/tap fallback mitigates accessibility, but the
  pointer-drag path (recently reworked) still warrants real iOS/Android Safari/Chrome testing.
- **No interaction-layer tests.** CI guards the pure logic, but a regression in the large `App.tsx`
  (drag/plot/slider/dragTerms) or in `MathText` rendering would pass CI undetected until manual QA.

---

## 9. Ways To Improve (prioritized recommendations)

**P0 — finish the launch gate**
1. Deploy to Firebase Hosting; verify rules + run a concurrency smoke test; enable App Check; validate
   the CSP on the live app.
2. ~~Fix the Firebase-SDK chunking regression (§6.1)~~ — **done this pass** (SDK is now lazy).
3. Implement password reset (R5).

**P1 — correctness & quality**
4. ~~Add unit tests for the `equationToLatex` path (§6.4)~~ — **done this pass**; still add a
   DOM/RTL harness for the `MathText` component itself.
5. Replace the cumulative-ratio mastery with EWMA and attribute attempts to the specific skill a step
   exercises (per-step `skillId`), so per-skill mastery is meaningful.
6. ~~Add `checkMcqStep` to `engine.ts` with tests; delete the duplicated view-side escalation~~ —
   **done this pass.**
7. Refresh `README.md`, `BACKEND_ADAPTERS.md`, and `SECURITY.md` to match the current six authored
   lessons, the four interaction types, KaTeX rendering, firebase mode, CI, and the actual mastery model.
8. Add a React error boundary; wire basic error/performance monitoring.

**P2 — maintainability & polish**
9. Split `App.tsx` into per-screen / per-step-renderer modules (and extract the drag engine).
10. Add a JSDOM/RTL harness for the interaction components and `MathText`, and run it in CI to cover
    what the pure tests can't.
11. Remove dead code (`CourseLessonNode.status`, the unreachable "Coming soon" branch) or wire it to
    real status.
12. Keep the pinned Node off the broken 24.17.0 patch and ensure deploys use the keep-alive wrapper.

---

## 10. Quick Reference

- **Run locally:** `npm install` → `npm run dev` (Vite prints the URL).
- **Verify:** `npm test` (179 tests) · `npm run lint` · `npm run build`.
- **CI:** `.github/workflows/ci.yml` (lint+build+test), `security.yml` (npm audit + CodeQL),
  `dependabot.yml` (weekly grouped updates).
- **Backend switch:** `VITE_BACKEND_PROVIDER` (`local` default, passwordless demo; `firebase` =
  real Auth + Firestore, fails closed until configured). Env vars in `.env.example`.
- **Key files:** content `src/content/` (types + per-lesson files); logic `src/engine.ts`; contract +
  local store `src/backend.ts`; Firebase adapter `src/firebaseBackend.ts`; UI `src/App.tsx`; equation
  rendering `src/MathText.tsx` + `src/equationLatex.ts`; auth validation `src/authValidation.ts`;
  rules `firestore.rules`; hosting/headers `firebase.json`; bundling `vite.config.ts`.
